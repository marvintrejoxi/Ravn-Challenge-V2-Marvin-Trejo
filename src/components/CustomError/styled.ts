import styled from 'styled-components';

export const ErrorWrapper = styled.p`
  font-style: normal;
  font-weight: bold;
  font-size: 17px;
  line-height: 20px;
  color: var(--red);
  letter-spacing: 0.0125em;
  text-align: center;
  margin: 16px 0 0;
`;
